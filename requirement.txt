1. Install open-whisper
2.install ffmpeg



//runtime/environment error:

1.(SSL certificate error — the specific error)In simple words:Your Python program tried to access a HTTPS website, 
but Python couldn't verify that the website's SSL certificate was trusted.

2.FFmpeg is a (system-level) program, so Homebrew installs it for your Mac and makes it 
available to programs like Whisper.

